numpy>=1.26
pandas>=2.0
scikit-learn>=1.5
matplotlib>=3.8
seaborn>=0.13
imbalanced-learn>=0.12,<0.14
shap>=0.46,<0.55
torch>=2.0,<3.0
pillow>=10.0
joblib>=1.3
tqdm>=4.66
jupyter>=1.0

# Optional — uncomment if you want gradient boosting via XGBoost rather than
# scikit-learn's HistGradientBoostingClassifier (the notebooks use HGB by default).
# xgboost>=2.0,<3.0
